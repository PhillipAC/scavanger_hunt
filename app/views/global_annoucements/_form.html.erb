<%= form_for(@global_annoucement) do |f| %>
  <% if @global_annoucement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@global_annoucement.errors.count, "error") %> prohibited this global_annoucement from being saved:</h2>

      <ul>
      <% @global_annoucement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
